# Harness Engineering in Pi (Slide Deck)

## What's here

- `index.html` — Reveal.js slide deck
- `assets/style.css` — custom styling
- `.github/workflows/pages.yml` — GitHub Pages deploy workflow

## Local preview

From this directory:

```bash
npx serve .
```
